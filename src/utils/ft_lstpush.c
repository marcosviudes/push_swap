#include "stacklib.h"

// a y b
// si no hay b no hace nada
//pone el primero de b en a

int		ft_lstpush(t_list **to_list, t_list *list)
{
	void	*temp;

	if(list = NULL)
        return(0);
	temp = list->next;
	

	return(1);
}